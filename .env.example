# HotDeploy local development flags (optional)
# Copy to .env — never commit real secrets

# VITE_* vars are exposed to the frontend bundle — do NOT put API keys here
VITE_APP_ENV=development

# GitHub App client ID for device flow (public). Run: pnpm register:github-app
# GITHUB_APP_CLIENT_ID=
